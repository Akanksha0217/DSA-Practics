# 🚀 Question

Find the next smaller element to the right for each element in an array

🧠 Problem

For every element, find the next element to its right which is smaller than it

If none exists → return -1
